# 更新日志

## 2025-08-28

### 新增

- **GUI 图表导出**
  - 每个图表页面新增“导出当前图片”和“保存全部图片”按钮。
  - 图片默认保存到 `data/plots/<run_id>/`，文件夹命名与实验 `run_id` 一致。
  - 保存图片时同时生成同名 `.json` 元数据文件，记录用到的数组名、形状、dtype 以及原始 `.npz` 数据路径。
  - 若目标目录/文件已存在，会弹窗询问是否覆盖。

- **GUI 图表数据导出**
  - 每个图表页面新增“导出当前图表数据”和“导出全部图表数据”按钮。
  - 导出格式为 `.xlsx`，不同数组放在不同 sheet。
  - 支持复数数组拆分为 real/imag 两列，兼容 0 维标量、1D/2D 数组。

- **GUI 离线运行定位**
  - “运行测试”页支持分别填写 STEP2（QPSK）和 STEP4（Bitloading）波形 ID 后六位。
  - 支持粘贴完整 run-id / 文件名，自动识别阶段并去掉 `.txt` / `.json` 等后缀。
  - 离线运行时优先使用源 RX 波形文件自身的 `run_id`，保持编号一致。

- **GUI 参数控制**
  - `config.py` 中的 `RATIO` 等参数可直接在 GUI 的“运行测试”页参数面板中修改并保存。

- **Windows 任务栏图标**
  - 设置显式 `AppUserModelID` 并生成多尺寸 `.ico` 图标，尝试解决任务栏仍显示默认羽毛的问题。

### 修复

- 修复导出 bit/power loading 等图表时偶发的 `tuple index out of range` 错误：
  - 0 维标量数组单独处理，避免访问 `arr.shape[0]`。
  - `bbox_inches="tight"` 保存失败时自动回退到普通保存。
  - Excel 导出时单个数组写入失败不再导致整体失败，其他数组仍可正常导出。

### 改进

- GUI 启动时自动清理 `data/plots/` 下的空文件夹。
- 实验下拉框基于 `data/records/` 去重后按时间倒序排列，单步运行生成的实验也能看到。
- 导出/保存失败时弹窗显示完整 Traceback，便于定位问题。
