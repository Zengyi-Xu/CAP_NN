numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0
PyVISA>=1.12.0
PyVISA-py>=0.7.0
pyserial>=3.5  # Keithley 2400 RS-232 / USB-to-RS232 control
tqdm>=4.62.0
matplotlib-inline>=0.1.0  # 用于在 Spyder/Jupyter 中显示图像
openpyxl>=3.0.0  # 导出图表数据为 Excel
Pillow>=8.0.0  # 把 emoji 渲染为窗口图标

torch>=2.0.0
# 注意：torch 建议根据 CUDA 版本安装；上述为 CPU 版本占位，CUDA 用户请自行换源
